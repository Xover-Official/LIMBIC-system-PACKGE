[project]
name = "consciousness-system"
version = "0.1.0"
description = "Global Workspace, Self-Model, and Metacognition for the Limbic System"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
